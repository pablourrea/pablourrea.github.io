/**
 * Provides custom single page functions metadata.
 */

scrollCue.init({
  docSlider : true,
  pageChangeReset : true,
});

docSlider.init({
  speed : 500,
  scrollReset : true,
  percentage : 0.8,
});
